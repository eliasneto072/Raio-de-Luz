# ===== Frontend (desenvolvimento) — Raio de Luz =====
# Com hot-reload via Vite

FROM node:20-slim

WORKDIR /app

COPY package*.json ./
RUN npm install

COPY . .

EXPOSE 5173

# Vite precisa do --host para ser acessível fora do container
CMD ["npm", "run", "dev", "--", "--host"]
